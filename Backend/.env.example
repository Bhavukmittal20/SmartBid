# Stripe test-mode secret key from https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_replace_me

# Signing secret from your Stripe webhook endpoint or `stripe listen`
STRIPE_WEBHOOK_SECRET=whsec_replace_me

# Frontend origin used for Checkout success and cancellation redirects
FRONTEND_URL=http://localhost:5173

# Optional marketplace fee percentage. Keep 0 to route the full winning bid to the seller.
STRIPE_PLATFORM_FEE_PERCENT=0
